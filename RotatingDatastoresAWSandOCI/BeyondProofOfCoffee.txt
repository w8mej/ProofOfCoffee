Production Hardening Checklist (beyond PoC)
Lambda VPC + security groups if RDS is private; add ENI/IAM permissions.

S3 SSE-KMS, Object Lock, and Block Public Access.

KMS key rotation and tight key policies.

EventBridge DLQ + alerting (SNS/PagerDuty).

Reserved concurrency for Lambda (serialize rotations).

Terraform backend (S3/DynamoDB) and state encryption.

SSM/SM–sourced runtime secrets (no inline salts/tokens).

CI gates: image scan, SBOM, cosign signing, and policy checks before terraform apply.

PubSubHubbub async events triggering adaptable behaviors. 